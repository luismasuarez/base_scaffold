import React from 'react';
import Box from '../../../core/components/Box/Box';
import Typography from '../../../core/components/Typography/Typography';

const DateHistory = () => {
  return (
    <Box>
      <Typography>DateHistory</Typography>
    </Box>
  );
};

export default DateHistory;
